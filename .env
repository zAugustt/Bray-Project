# Please fill out empty environment variables (those with no value after = symbol)


# Developer note: These values may only be used in the Docker Compose file (not Dockerfiles) 
#                 unless otherwise specified by env_file.

POSTGRES_DB=sensor_info_db
POSTGRES_USER=bray_tamu
POSTGRES_PASSWORD=test_password
POSTGRES_HOST=db
POSTGRES_PORT=5432

MQTT_HOST=mosquitto
MQTT_USERNAME=bray_tamu
MQTT_PASSWORD=password
